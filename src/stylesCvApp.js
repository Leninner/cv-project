import styled from 'styled-components';

export const CvComponentContainer = styled.div`
  display: flex;
  min-height: 100vh;
  height: auto;

  @media (max-width: 768px) {
    display: block;
  }
`;
